import { useNavigate, useParams } from "react-router-dom";


function Countries() {
  const params = useParams();
  const navigate = useNavigate();
     
      return (
        <> 
        <h1>Country Details</h1>   s
        </>
      );
  }

export default Countries;
        
    